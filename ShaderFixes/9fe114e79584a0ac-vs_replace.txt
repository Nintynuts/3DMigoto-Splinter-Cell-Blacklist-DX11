// GUI - Text Batched

cbuffer CB_VS_ScaleformBatched : register(b7)
{
	float4 mvp[192] : packoffset(c0);
}

#include "CB_PerCamera.hlsl"

#include "3Dmigoto.hlsl"

#include "goal_boundaries.hlsl"

void main(
	float4 v0 : POSITION0,
	float2 v1 : TEXCOORD0,
	float4 v2 : COLOR0,
	float4 v3 : COLOR1,
	uint vID : SV_VertexID,
	out float4 o0 : SV_Position0,
	out float4 o1 : TEXCOORD0,
	out float4 o2 : TEXCOORD1,
	out float4 o3 : TEXCOORD2,
	out float4 o4 : COLOR0)
{
	float4 r0;
	
	r0.x = 2040.01001 * v3.x;
	r0.x = (int)r0.x;
	o0.x = dot(v0, mvp[r0.x + 0]);
	o0.y = dot(v0, mvp[r0.x + 1]);
	o0.zw = v0.zw;
	o1.xy = v1.xy;
	o2 = mvp[r0.x + 5];
	o3 = mvp[r0.x + 4];
	o4 = v2.zyxw;
	
	if (!rt_res_size_match()) // Render Target
		return;

	o0.x += depth[0] * -eye;

	return;
}